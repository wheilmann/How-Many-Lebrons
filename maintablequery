SELECT 
  year,
  SUM(CASE WHEN name = "Lebron" THEN number ELSE 0 END) AS Lebrons,
  SUM(CASE WHEN name = "Giannis" THEN number ELSE 0 END) AS Giannises
FROM `bigquery-public-data.usa_names.usa_1910_current` 
WHERE
  name IN ("Lebron", "Giannis")
GROUP BY 
  year
ORDER BY
year
